# Copy to .env (gitignored) and fill in. Only needed for the live adapter and
# the LLM judge; the replay adapter and the CI gate need nothing.
#
# An exported ANTHROPIC_API_KEY always wins over this file.
ANTHROPIC_API_KEY=sk-ant-...
